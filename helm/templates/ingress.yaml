apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
  rules:
  - host: koval92.xyz
    http:
      paths:
      - backend:
          serviceName: service
          servicePort: 80
